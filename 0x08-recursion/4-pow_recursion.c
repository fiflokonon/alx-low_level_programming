#include "main.h"
#include <math.h>

/**
 * _pow_recursion - a function that prints a string, followed by a new line.
 * @s: An input string to printing
 * Return: Nothing
 */
void _put_recursion(int x, int y)
{
	return (pow(x, y));
}
